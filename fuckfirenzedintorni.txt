! Title: fuckfirenzedintorni
! Description: Filter to block ads on Firenze e Dintorni's website, might break from time to time
! Author: JunjoSick
! Version: 1.1.2
! Expires: 30 days
! Homepage: https://github.com/JunjoSick/filtershosting
! Licence: https://github.com/JunjoSick/filtershosting/blob/main/LICENSE
! RAW: https://raw.githubusercontent.com/JunjoSick/filtershosting/main/fuckfirenzedintorni.txt

! Sponsor logo carousel creatives (sidebar on home, mid-article placements)
||firenzedintorni.it/uploadedfiles/sponsor/$image
firenzedintorni.it##.itm_sponsor_carousel
! The grey box is a dedicated sponsor wrapper on every audited page; plain CSS so
! clients without ExtendedCss/procedural support still remove it fully, margins included
firenzedintorni.it##.bkg-griginoo

! Cookiekit consent notice (custom WDE banner, moved before the footer by JS)
firenzedintorni.it###wdc_banner
firenzedintorni.it##.cookiebanner

! Articles indent some paragraphs with leading &nbsp; runs, breaking left alignment.
! Strip runs of 2+ non-breaking spaces at the start of article body spans.
! The two engines have incompatible signatures for this scriptlet, hence two rules.
! Trusted scriptlet: mark this list as trusted in uBO (Trusted checkbox) for it to apply.
! uBO signature: selector, textPattern, flags, replaceThis, withThis
firenzedintorni.it##+js(trusted-replace-node-text, .post-content .mb-4 span, .+, , ^\u00A0{2,}, )
! AdGuard signature: nodeName, textMatch, pattern, replacement (regexes in /.../ form)
! textMatch must equal the strip pattern: AdGuard rewrites node.textContent even when
! unchanged, which would delete <br> children of every span if textMatch matched them all.
firenzedintorni.it#%#//scriptlet('trusted-replace-node-text', 'span', '/^\u00A0{2,}/', '/^\u00A0{2,}/', '')

! Dashed "functionality disabled until cookies" placeholders replacing share buttons
firenzedintorni.it#?#div:has(> .cookiekit_accetto_btn)
firenzedintorni.it##div:has(> .cookiekit_accetto_btn)
